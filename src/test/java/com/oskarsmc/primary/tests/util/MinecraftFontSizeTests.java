package com.oskarsmc.primary.tests.util;

public class MinecraftFontSizeTests {
    /* I need to learn to write tests.
    @Test
    @DisplayName("Ensure the correct amount of spaces when centering.")
    void testComponentCentering() {
        assertTrue(
                PlainTextComponentSerializer.plainText().serialize(MinecraftCharacter.getMOTDCenteredMessage(
                        Component.text("Hello, World!")
                )).contains("      ")
        );
    }
     */
}
